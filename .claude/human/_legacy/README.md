# _legacy — Mirror VI cũ của agent definitions (NGƯNG CẬP NHẬT)

> Mirror tiếng Việt cũ của agent definitions / commands / templates / glossary / knowledge / examples — **ngưng cập nhật từ 2026-07-02 theo quyết định BA Lead**.
>
> Giữ tạm để tham khảo. BA Lead xóa sau khi duyệt cấu trúc mới của `.claude/human/` (chân dung vai trò con người trong dự án).

| Thư mục con | Nội dung cũ |
|---|---|
| `agents/` | Mirror VI của 10 agent definitions (`.claude/agents/`) |
| `commands/` | Mirror VI của 5 slash commands (`.claude/commands/`) |
| `templates/` | Mirror VI của BA templates (`.claude/templates/ba/`) |
| `glossary/` | Mirror VI của từ điển thuật ngữ (`.claude/glossary/`) |
| `knowledge/` | Mirror VI của một số file knowledge (`.claude/knowledge/`) |
| `examples/` | Mirror VI của ví dụ BA (`.claude/examples/`) |

Bản canonical (EN) tại `.claude/{agents,commands,templates,glossary,knowledge,examples}/` **vẫn hoạt động bình thường** — chỉ nghĩa vụ mirror VI bị bãi bỏ (xem `.claude/sync/SYNC-PROTOCOL.md` và SYNC-LOG entry 2026-07-02).
